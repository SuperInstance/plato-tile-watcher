[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-watcher"
version = "0.1.0"
description = "Tile lifecycle monitoring"
license = {text = "MIT"}
requires-python = ">=3.8"
